# Optional: generate sample parquet with pandas for local testing, e.g.:
# import numpy as np
# import pandas as pd
# df = pd.DataFrame({"pct_2013": np.random.rand(10)})
# df.to_parquet("test_data.parquet", index=False)
